{
  "name": "dynamodb-with-aws-sdk-v3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && docker-compose up --force-recreate",
    "build": "npm run clear && tsc && docker-compose build --no-cache",
    "clear": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb-v2-node": "0.1.0-preview.2",
    "aws-sdk": "^2.608.0"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  }
}
